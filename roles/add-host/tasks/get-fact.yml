---
# tasks file for add-host

- name: Collect only facts returned by facter
  setup:
    gather_subset:
      - '!all'
      - '!any'
      - network
      - facter
  register: vminfo

- debug: var=vminfo.ansible_facts.ansible_os_family
  when: vminfo.ansible_facts.ansible_os_family == "Windows"
